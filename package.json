{
  "name": "rs596-consulta-fichadas",
  "version": "1.0.0",
  "private": true,
  "description": "Script CLI para consultar fichadas pendientes del reloj biometrico Prosoft RS596 via su protocolo TCP propietario.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/cli/consultar-fichadas.js",
    "servicio": "node --env-file-if-exists=.env src/cli/consulta-programada.js",
    "test": "node --test",
    "smoke:oracle": "node --env-file-if-exists=.env scripts/smoke-oracle-roster.js"
  },
  "dependencies": {
    "oracledb": "^6.5.1"
  }
}
